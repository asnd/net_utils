stages:
  - build
  - test

before_script:
  - go version
  - go mod download

build_linux:
  stage: build
  tags:
    - linux
  script:
    - echo "Building on Linux x86"
    - GOOS=linux GOARCH=386 go build -o strip_pcap_linux_x86 strip_pcap.go



